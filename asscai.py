data = input("Enter the value you want to change...")

print(data)

for pross in data:
    print(f'assic value is {ord(pross)} The binary value is {bin(ord(pross))} the memory is {id(ord(pross))}')
